<div class="container">
	<div class="row">
		<div class="col-md-12">
			<h1 class="col-md-2 col-md-offset-5">商品一覧</h1>
			<div class="title col-md-12"></div>
		</div>
	</div>

	<div class="row col-md-12">
		<%= form_tag(items_path, method: "get") do %>
			<%= text_field_tag :search, params[:search], class: "searchbar col-md-8 col-md-offset-2", placeholder: "検索..." %>
			<%= submit_tag "検索", class: "btn btn-success" %>
		<% end %>
	</div>

	<div class="row">
		<table class="table table-hover">
			<thead>
				<tr>
					<th class="center">アルバム名</th>
					<th class="center">アーティスト名</th>
					<th class="center">ジャンル</th>
					<th class="center">販売日</th>
					<th class="center">在庫数</th>
					<th class="center" >販売状況</th>
					<th class="center">価格</th>
					<th class="center">存在</th>
					<th colspan="3">アクション</th>
				</tr>
			</thead>
			<tbody>
				<% @items.each do |item| %>
				<tr>
					<td class="center"><%= item.album_name %></td>
					<td class="center">
						<ruby>
							<%= item.artist_name %> 
								<rt><%= item.artist_name_kana %></rt>
						</ruby>
					</td>
					<td class="center"><%= item.genre %></td>
					<td class="center"><%= item.luanch_date.strftime('%Y/%m/%d') %></td>
					<td class="center"><%= item.stock %></td>
					<td class="center"><%= item.item_status %></td>
					<td class="center">¥ <%= item.price.to_s(:delimited) %></td>
					<td class="center"><%= item.active %></td>
					<td><%= link_to item_path(item) do %><i class="fa fa-window-maximize" aria-hidden="true"></i><% end%></td>
					<td><%= link_to edit_item_path(item) do %><i class="fa fa-pencil" aria-hidden="true"></i><% end %></td>
					<td><%= link_to item_path(item), method: :delete, "data-confirm" => "Are you sure?" do %><i class="fa fa-trash-o" aria-hidden="true"></i><% end %></td>
				</tr>
				<%end%>
			</tbody>
		</table>
	</div>
</div>


